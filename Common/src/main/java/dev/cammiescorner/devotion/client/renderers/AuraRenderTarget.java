package dev.cammiescorner.devotion.client.renderers;

import com.mojang.blaze3d.pipeline.RenderTarget;

public class AuraRenderTarget extends RenderTarget {
	public AuraRenderTarget(boolean useDepth) {
		super(useDepth);
	}
}
